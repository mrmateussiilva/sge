services:
  sge:
    build: .
    ports:
      # Expõe apenas para o localhost (Caddy no host acessa, internet não)
      - "127.0.0.1:8000:8000"
    env_file: .env
    volumes:
      - sqlite_data:/app/data
    restart: unless-stopped

volumes:
  sqlite_data:
